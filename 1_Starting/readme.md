To create angular project we need (node) **nvm** -> node version manager.

*To Install go to browser type -> nvm for windows -> from github download and install it.

once installed verify it in cmd nvm -v.

*Now install node in devide by typing **nvm install lts**
it installs latest long term suppor node version 
check it by **node --version**


**go to directory where you want to install angular**
type -> **npm install -g @angular/cli**

this is angulars command line interface used to run and do multiple things in angular 

*to create a angular project go to directrey and type in terminal

**ng new nameofProj**  

to run -> in terminal  **npm run**